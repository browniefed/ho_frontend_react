var React = require('react/addons');


var Title = React.createClass({
    render: function() {
        return (
            <h2 {...this.props} className="candidate-name">
                {this.props.children}
            </h2>
        )
    }
});

module.exports = Title;