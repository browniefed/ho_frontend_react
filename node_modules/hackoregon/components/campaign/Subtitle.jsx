var React = require('react/addons');


var Subtitle = React.createClass({
    render: function() {
        return (
            <h4 {...this.props}>
                {this.props.children}
            </h4>
        )
    }
});

module.exports = Subtitle;