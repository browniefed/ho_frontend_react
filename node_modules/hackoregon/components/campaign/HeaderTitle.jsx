var React = require('react/addons');


var HeaderTitle = React.createClass({
    render: function() {
        return (
            <h1 {...this.props} className="candidate-name">
                {this.props.children}
            </h1>
        )
    }
});

module.exports = HeaderTitle;